@page
@model IndexModel
@{
    ViewData["Title"] = "TODO App";
}

<div class="hero-section" style="display: flex; justify-content: center; align-items: center; flex-direction: column; min-height: 100vh; text-align: center;">
    <h1 class="display-3" style="font-weight: bold; margin-bottom: 20px;">Welcome to TODO App</h1>
    <p style="font-size: 1.5rem; margin-bottom: 30px;">Stay on top of your tasks with ease and elegance.</p>

    <div style="margin-bottom: 20px;">
        @if (HttpContext.Session.GetString("UserRole") == null)
        {
            <a class="btn btn-primary btn-lg" href="/Account/SignUp" style="margin: 10px; padding: 15px 30px; border-radius: 30px; font-size: 1.2rem;">Get Started</a>
        }
        @if (HttpContext.Session.GetString("UserRole") != null){
        <a class="btn btn-outline-light btn-lg" href="/Task/Index" style="margin: 10px; padding: 15px 30px; border-radius: 30px; font-size: 1.2rem;">Explore Tasks</a>
        }
    </div>
</div>
